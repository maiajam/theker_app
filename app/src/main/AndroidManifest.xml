<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.maiajam.counter">

    <uses-permission
        android:name="android.permission.VIBRATE"></uses-permission>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="سبحتي"
        android:supportsRtl="false"
        android:roundIcon="@mipmap/logo"
        android:theme="@style/AppTheme">
        <activity android:name=".WelcomeActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar" android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".listActivity"
            android:noHistory="true"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".CounterActivity"
            android:parentActivityName=".listActivity" android:noHistory="true" android:screenOrientation="portrait">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.maiajam.counter.listActivity"   android:screenOrientation="portrait"/>
        </activity>
        <activity android:name=".ThekerTargetActivity" android:noHistory="true" android:theme="@style/Theme.AppCompat.DayNight.NoActionBar" >


        </activity>

        <service android:name=".Schudle" android:permission="android.permission.BIND_JOB_SERVICE"/>
    </application>

</manifest>